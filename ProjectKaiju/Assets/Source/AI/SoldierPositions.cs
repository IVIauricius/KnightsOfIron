using System.Collections;
using System.Collections.Generic;
using UnityEngine;

// I keep track of the positions the AI needs to follow the mech.

public class SoldierPositions : MonoBehaviour {
    public GameObject leftRear;
    public GameObject leftForward;
    public GameObject rightRear;
    public GameObject rightForward;

    // Use this for initialization
    void Start () {
		
	}
	
	// Update is called once per frame
	void Update () {
		
	}
}